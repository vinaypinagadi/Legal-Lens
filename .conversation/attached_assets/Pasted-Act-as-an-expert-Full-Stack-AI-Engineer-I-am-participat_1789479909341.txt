Act as an expert Full-Stack AI Engineer. I am participating in a hackathon to build a GenAI-powered legal assistant. I need you to generate the code, folder structure, database schema, and configuration files for my project.

PROJECT OVERVIEW
Name: "LegalLens - AI Contract Analyzer"
Vertical: Simplifying complex legal documents and highlighting risks/obligations.
Goal: A web application where non-lawyers can upload or paste a legal document. The AI will simplify the jargon, flag potential risks, and allow the user to chat with the document. 

TECH STACK
- Frontend: Next.js (App Router), Tailwind CSS, Lucide React (for icons)
- Database & Auth: Supabase (using `@supabase/supabase-js` and `@supabase/ssr`)
- AI Integration: `@google/genai` (The official Google Gen AI SDK for Gemini)
- File Processing: pdf-parse (or a similar lightweight PDF text extractor)

CORE FEATURES & ARCHITECTURE
1. Mandatory Legal Disclaimer: A banner fixed at the top stating: "LegalLens provides AI-generated legal information, not professional legal advice. Always consult a licensed attorney."
2. Bring Your Own Key (BYOK): 
   - Users must be prompted to enter their own Gemini API Key (obtained from Google AI Studio) before analyzing documents.
   - Store this key securely in the user's active session state or encrypt it in Supabase tied to their user ID.
   - The Next.js backend API routes must dynamically initialize the Gemini client using the user-provided key for each request (`new GoogleGenAI({ apiKey: userProvidedKey })`).
3. Supabase Database Schema: Generate the SQL schema to create:
   - `users` (managed by Supabase Auth)
   - `documents` (stores the extracted text of the uploaded contracts, tied to user_id)
   - `chat_history` (stores the Q&A threads, tied to document_id)
4. Analysis Dashboard: Once the document is processed, generate a UI with 3 tabs:
   - "TL;DR Summary" (Explain like I'm 5)
   - "Red Flags & Risks" (Bullet points of hidden obligations)
   - "Q&A Chat" (A chat interface to ask questions about the text)

HACKATHON CONSTRAINTS & INSTRUCTIONS (STRICT)
1. Repository Size Limit (< 10MB): You MUST generate a rigorous `.gitignore` file that excludes `node_modules`, `.next`, virtual environments, `.env` files, and any large PDF test files. 
2. Single Branch: Generate standard git initialization commands ensuring everything goes to the `main` branch.
3. Clean Code: Ensure code is highly modular. Separate UI components, utility functions, and AI API routes.
4. README.md: Generate a comprehensive README.md that includes:
   - Project Name & Description
   - The chosen vertical (Simplifying complex legal documents & Risk Analysis)
   - Approach, logic, and architecture (mentioning Supabase and the BYOK Gemini setup)
   - Step-by-step local setup instructions (including Supabase project setup and table creation)
   - Assumptions made during development

Please provide:
1. The `.gitignore` file.
2. The `README.md`.
3. The Supabase SQL schema setup script.
4. The core code files: the BYOK input component, the API routes for the Gemini LLM, the main UI page, and the Supabase client utility file.